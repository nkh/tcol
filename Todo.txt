
small swatch

bg/fg colors parsed
	from input on the left side
	fzf input
		user_color go to change side not picked side

	from clipboard
		when stated via tmux shortcut
		
	allows us to set fg and bg so the chosing starts at the color no (0, 0)
